# Archived Planning Notes

These files are historical implementation and migration planning context only.
They are not runnable operator instructions, active config guidance, or current
acceptance source of truth.

Current sources of truth are the root `README.md`, root `REVIEW.md`,
`AGENTS.md`, and the active docs under `docs/`.
