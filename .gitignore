# Build outputs
**/build/
**/build*/
**/cmake-build-*/
**/CMakeFiles/
*.egg-info/
**/CMakeCache.txt
**/CTestTestfile.cmake
**/cmake_install.cmake
**/compile_commands.json

# Local secrets — sudo password for setcap automation (NEVER commit)
.env
/.env

# Logs and runtime artifacts
**/logs/
**/*.log
**/*.tmp
**/artifacts/
**/episodes/
**/checkpoints/
outputs/

# Python caches
**/__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local agent/runtime state
.omx/
**/.omx/
**/**/**/.claude/settings.local.json

# Local/vendor integration checkouts
rb_servo_server/submodules/

# Migration backups

# OS/editor files
.DS_Store
*.swp
*.swo
.vscode/
.idea/

data*/

# Local HDF5 datasets
*.hdf5
*.h5

# PyTorch model files
*.pth
*.pt
*.onnx
*.engine

# Local Python virtualenvs
.venv/
venv/

# Documents
*.pdf
# cm_bridge 네이티브 플랫폼 디렉터리의 작업용 디바이스 파일.
# 추적 템플릿은 cm_bridge/config/active.monkey.real.yaml 이고, 런처가 여기로 설치한다.
# controller-manager 가 박스에서 채택한 DH 를 이 파일에 되쓰므로 추적하지 않는다.
cm_bridge/config/monkey/active.yaml
cm_bridge/config/monkey-sils/active.yaml
